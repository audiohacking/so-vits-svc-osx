<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Audio/microphone access for voice recording -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    
    <!-- Camera access (if needed for future features) -->
    <key>com.apple.security.device.camera</key>
    <true/>
    
    <!-- Network access for model downloads and updates -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Local network access for localhost server -->
    <key>com.apple.security.network.server</key>
    <true/>
    
    <!-- File system access for reading/writing audio files and models -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    
    <!-- Allow JIT compilation for PyTorch on Apple Silicon -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow unsigned executable memory (required for some ML libraries) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Disable library validation for third-party libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Hardened Runtime -->
    <key>com.apple.security.cs.hardened-runtime</key>
    <true/>
</dict>
</plist>
